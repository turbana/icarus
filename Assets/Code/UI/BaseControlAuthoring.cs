using UnityEngine;

namespace Icarus.UI {
    public class BaseControlAuthoring : MonoBehaviour {
        [Tooltip("Identifier of the Datum this control will write to.")]
        public string DatumID;
    }
}
